import React from 'react';
import styled from 'styled-components';

const GridWrapper = styled.div`
  margin-left: 5em;
  padding: 50px 0;
`;

export const Balance = (props) => (
  <GridWrapper>
    <p>Это страница баланса личного кабинета</p>
    <p>Но пока что тут ничего нет!</p>
  </GridWrapper>
)